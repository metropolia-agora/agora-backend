name: build-and-deploy
on:
  push:
    branches: [deployment]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH Remote Commands
        uses: appleboy/ssh-action@v0.1.4
        with:
          host: "shell.metropolia.fi"
          username: "${{ secrets.SSH_METROPOLIA_USERNAME }}"
          password: "${{ secrets.SSH_METROPOLIA_USERNAME }}"
          proxy_host: "10.114.34.84"
          proxy_username: "${{ secrets.SSH_SERVER_USERNAME }}"
          proxy_password: "${{ secrets.SSH_SERVER_PASSWORD }}"
          script: whoami
